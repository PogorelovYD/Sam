syntax = "proto3";

package game;

service GameLobby {
    rpc Matchmaking(stream PlayerInfo) returns (stream MatchResult);
}

message PlayerInfo {
    string nickname = 1;
    int32 level = 2;
}

message MatchResult {
    string message = 1;
}